import { Bot } from 'lucide-react';
import ComingSoonPage from "../components/common/ComingSoonPage"

export default function AgentsPage() {
  return (
    <ComingSoonPage
      icon={Bot}
      title="Agents"
      description="Create and manage your AI agents with custom instructions and capabilities."
    />
  );
}
