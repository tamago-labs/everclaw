// Sanctum LST swap execution service

import { executeTransaction } from '../execute';

// Re-export executeTransaction for backward compatibility
export { executeTransaction as executeSanctumSwap };
